#! /bin/bash


# settings ------------------------------------------------------------------------------------------

version="3.13"
python3=$( command -v "python${version}" )


# venv ----------------------------------------------------------------------------------------------

venv_dir="${_root}/.venv"


# pip -----------------------------------------------------------------------------------------------

pip_requirement_d="${_root}/etc/pip/requirement.d/${version}"
bootstrap="${pip_requirement_d}/bootstrap"
root="${pip_requirement_d}/root"
versions="${pip_requirement_d}/versions"
pylock_toml="${pip_requirement_d}/pylock.toml"

